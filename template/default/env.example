NEXT_PUBLIC_FILECOIN_NETWORK=__FOC_NETWORK__

# Optional public address for read-only `foc:doctor:online` balance checks.
# FOC_WALLET_ADDRESS=0x...

# Node-only and optional. Used by the live smoke test and read-only online doctor checks.
# Never prefix this variable with NEXT_PUBLIC_.
# FOC_SMOKE_PRIVATE_KEY=0x...
